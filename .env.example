# API keys
VIRUSTOTAL_API_KEY=replace_me
SAFE_BROWSING_API_KEY=replace_me
PHISHTANK_API_KEY=optional
ABUSEIPDB_API_KEY=optional
GEMINI_API_KEY=replace_me

# Email alerts (Brevo)
BREVO_API_KEY=optional
ALERT_EMAIL=alerts@example.com
SENDER_EMAIL=notifier@example.com
BREVO_SENDER=notifier@example.com
BREVO_RECEIVER=alerts@example.com

# Flask
DEBUG=True
DATABASE_URL=sqlite:///database/threats.db

# Gmail (email scanner)
# Place OAuth client file at backend/credentials/credentials.json
# token.pickle will be created after first auth flow

# Browser extension / webhook
# Add any custom webhook secrets or CORS origins here if needed
